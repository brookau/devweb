define(function(require) {
    'use strict';

    var angular = require('angular'),
        controller;

    controller = ['$scope', '$http',
        function ($scope, $http) {}
    ];

    return controller;
});