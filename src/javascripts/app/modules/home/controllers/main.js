/* istanbul ignore next */
define(function (angular) {
  'use strict';

  var controller = ['$scope', '$sce', 'feedFactory',
    function ($scope, $sce, feedFactory) {

    }
  ];

  return controller;
});
